---
description: Run Expo app on all devices (web and Android) simultaneously
---
// turbo-all
1. Start Expo development server in background
```bash
expo start
```
2. Open web version in browser
```bash
expo start --web
```
3. Build and run Android app (requires Android emulator/device)
```bash
expo run:android
```
